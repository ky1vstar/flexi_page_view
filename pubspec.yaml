name: flexi_page_view
description: A Flutter widget that dynamically adjusts its height to match the height of the currently visible page, serving as a drop-in replacement for PageView.
version: 1.0.0
homepage: https://github.com/ky1vstar/flexi_page_view
repository: https://github.com/ky1vstar/flexi_page_view
issue_tracker: https://github.com/ky1vstar/flexi_page_view/issues

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.3.0
